#!/usr/bin/env python
# -*- coding: utf-8 -*-
# generated by wxGlade 0.6.3 on Tue Apr 13 11:20:19 2010

import wx

# begin wxGlade: extracode
# end wxGlade



class MyFrame(wx.Frame):
    def __init__(self, *args, **kwds):
        # begin wxGlade: MyFrame.__init__
        kwds["style"] = wx.DEFAULT_FRAME_STYLE
        wx.Frame.__init__(self, *args, **kwds)
        self.entradatex = wx.TextCtrl(self, -1, "", style=wx.TE_MULTILINE)
        self.salidatex = wx.TextCtrl(self, -1, "", style=wx.TE_MULTILINE)

        self.__set_properties()
        self.__do_layout()

        self.Bind(wx.EVT_TEXT_URL, self.urlin, self.entradatex)
        self.Bind(wx.EVT_TEXT_MAXLEN, self.maxlenin, self.entradatex)
        self.Bind(wx.EVT_TEXT_ENTER, self.enterin, self.entradatex)
        self.Bind(wx.EVT_TEXT, self.texin, self.entradatex)
        self.Bind(wx.EVT_TEXT_URL, self.evttexturlsal, self.salidatex)
        self.Bind(wx.EVT_TEXT_MAXLEN, self.etexmaxlensal, self.salidatex)
        self.Bind(wx.EVT_TEXT_ENTER, self.evttextentersal, self.salidatex)
        self.Bind(wx.EVT_TEXT, self.evttextsal, self.salidatex)
        # end wxGlade

    def __set_properties(self):
        # begin wxGlade: MyFrame.__set_properties
        self.SetTitle("frame_1")
        # end wxGlade

    def __do_layout(self):
        # begin wxGlade: MyFrame.__do_layout
        sizer_1 = wx.BoxSizer(wx.VERTICAL)
        sizer_1.Add(self.entradatex, 1, wx.EXPAND, 0)
        sizer_1.Add(self.salidatex, 4, wx.EXPAND, 6)
        self.SetSizer(sizer_1)
        sizer_1.Fit(self)
        self.Layout()
        # end wxGlade

    def urlin(self, event): # wxGlade: MyFrame.<event_handler>
        print "Event handler `urlin' not implemented!"
        event.Skip()

    def maxlenin(self, event): # wxGlade: MyFrame.<event_handler>
        print "Event handler `maxlenin' not implemented!"
        event.Skip()

    def enterin(self, event): # wxGlade: MyFrame.<event_handler>
        print "Event handler `enterin' not implemented!"
        event.Skip()

    def texin(self, event): # wxGlade: MyFrame.<event_handler>
        print "Event handler `texin' not implemented!"
        event.Skip()

    def evttexturlsal(self, event): # wxGlade: MyFrame.<event_handler>
        print "Event handler `evttexturlsal' not implemented!"
        event.Skip()

    def etexmaxlensal(self, event): # wxGlade: MyFrame.<event_handler>
        print "Event handler `etexmaxlensal' not implemented!"
        event.Skip()

    def evttextentersal(self, event): # wxGlade: MyFrame.<event_handler>
        print "Event handler `evttextentersal' not implemented!"
        event.Skip()

    def evttextsal(self, event): # wxGlade: MyFrame.<event_handler>
        print "Event handler `evttextsal' not implemented!"
        event.Skip()

# end of class MyFrame


if __name__ == "__main__":
    app = wx.PySimpleApp(0)
    wx.InitAllImageHandlers()
    frame_1 = MyFrame(None, -1, "")
    app.SetTopWindow(frame_1)
    frame_1.Show()
    app.MainLoop()
